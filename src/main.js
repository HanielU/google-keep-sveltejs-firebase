import App from "./App.svelte";

// -> Default Settings
/* const app = new App({
	target: document.body,
	props: {
		name: 'world'
	}
}); */

const app = new App({
	target: document.body,
});

export default app;
